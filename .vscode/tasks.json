{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build my C++ project",
            "type": "shell",
            "command": "g++ ${file} -o main -Wall -std=c++20 ;; ./main",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}